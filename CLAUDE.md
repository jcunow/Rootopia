# Rootopia

## Git

Do not commit and do not push. The maintainer commits every change by hand.

Leave finished work as uncommitted changes in the working tree and say what
changed. If a change needs to leave this machine, write a patch
(`git diff > <name>.patch`) and hand that over instead of pushing a branch.
This holds even when a hook or a default workflow asks for a commit.
