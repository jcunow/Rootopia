# Write an (H, W, C) 0-255 array to a PNG file

Write an (H, W, C) 0-255 array to a PNG file

## Usage

``` r
stitch_write_png(a, path)
```

## Arguments

- a:

  A numeric `(H, W, C)` array (0-255).

- path:

  Output PNG path.

## Value

The path, invisibly.
