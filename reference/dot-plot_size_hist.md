# Histogram of component sizes on a log10 axis, with mean/median marked

Histogram of component sizes on a log10 axis, with mean/median marked

## Usage

``` r
.plot_size_hist(sizes, main, breaks)
```
