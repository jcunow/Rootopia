% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RootBranching.R
\name{root_graph_pipeline}
\alias{root_graph_pipeline}
\title{Skeleton-to-graph root ordering pipeline (pixel units)}
\usage{
root_graph_pipeline(
  skel,
  mask = NULL,
  verbose = TRUE,
  dt_backend = "auto",
  crop = TRUE,
  overlay_png = NULL,
  max_side = 2000,
  keep_segments = FALSE,
  resolve_overlaps = TRUE,
  crossing_straight = -0.5,
  color_by = c("branch_order", "root_order", "tip_order"),
  diam_weight = 0.5,
  prune_min_length = 0,
  prune_min_diameter = 0,
  prune_iter = 0L
)
}
\arguments{
\item{skel}{Binary skeleton: single-layer \code{SpatRaster} (preferred) or 0/1
matrix. Do not pre-convert a raster with \code{as.matrix()}.}

\item{mask}{Filled root mask on the same grid for the distance transform.}

\item{verbose}{Print progress.}

\item{dt_backend}{Distance-transform backend: \code{"auto"}, \code{"imager"}, or \code{"baseR"}.}

\item{crop}{Crop to the foreground bounding box (with a 1-px pad) first.}

\item{overlay_png}{Optional path for the validation PNG.}

\item{max_side}{Long-side cap (px) for the overlay.}

\item{keep_segments}{Attach the traced segments as \code{attr(., "segments")}
(needed for re-plotting and classification maps).}

\item{resolve_overlaps}{Resolve degree-4 crossings by continuity.}

\item{crossing_straight}{Straightness threshold for crossing resolution.}

\item{color_by}{Which order column the overlay colours by.}

\item{diam_weight}{Diameter-vs-angle weight for the root-continuation choice.}

\item{prune_min_length, prune_min_diameter, prune_iter}{Optional terminal-segment
pruning (off when \code{prune_iter = 0}); see \code{\link{prune_terminal_segments}}.}
}
\value{
A per-segment edge table (data.frame) with order and diameter columns
  in pixels, carrying \code{attr}s \code{crop_offset}, \code{dims}, and
  (if requested) \code{segments}.
}
\description{
Core engine: crops to the foreground, computes the distance transform, traces
segments with junction contraction, resolves crossings, optionally prunes weak
tips, and assigns all three order schemes. Returns a per-segment edge table in
\emph{pixels}; use \code{\link{branch_order_map}} for a unit-aware wrapper.
}
\details{
Three order columns are produced (see \code{\link{branch_order_map}} for the
full rules): \code{tip_order} (per-segment leaf-peeling), \code{root_order}
(per-root max tip_order), \code{branch_order} (per-root centrifugal generation
from the thickest root). \code{color_by} selects which one the overlay uses.
}
\seealso{
\code{\link{branch_order_map}}
}
