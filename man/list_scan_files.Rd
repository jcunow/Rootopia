% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stitch_scans.R
\name{list_scan_files}
\alias{list_scan_files}
\title{List scan files with index and group id}
\usage{
list_scan_files(input, pattern = NULL, group_regex = "T0\\\\d{2}")
}
\arguments{
\item{input}{Either a directory (searched recursively) or a character vector
of image file paths.}

\item{pattern}{Optional substring used to keep only matching file names
(e.g. \code{".tiff"}). \code{NULL} keeps all files.}

\item{group_regex}{Regular expression identifying the group id within each
path. Default \code{"T0\\d{2}"} matches tube labels such as \code{T067}.}
}
\value{
A data frame with columns \code{index} (1-based position), \code{file}
  (full path) and \code{group} (matched id, or \code{NA}).
}
\description{
Discovers the image files that \code{\link{stitch_root_scans}} would process
and returns them as an indexed table, so you can see what is in a folder and
pick a range of files to stitch (e.g. \code{select = 1:36}). For tube-level
selection see \code{\link{list_tubes}}. Uses the same discovery and sort order
as \code{stitch_root_scans}, so the indices line up.
}
\examples{
d <- file.path(tempdir(), "scans_demo")
dir.create(d, showWarnings = FALSE)
file.create(file.path(d, c("S_T037_01.tiff", "S_T037_02.tiff", "S_T038_01.tiff")))
list_scan_files(d, pattern = ".tiff")
unlink(d, recursive = TRUE)
}
\seealso{
\code{\link{list_tubes}}, \code{\link{stitch_root_scans}}
}
\keyword{internal}
