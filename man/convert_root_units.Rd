% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RootBranching.R
\name{convert_root_units}
\alias{convert_root_units}
\title{Convert edge-table lengths and diameters to real units}
\usage{
convert_root_units(
  et,
  unit = c("cm", "inch", "px"),
  dpi = 300,
  length_method = c("polyline", "kimura")
)
}
\arguments{
\item{et}{Edge table in pixels (from \code{\link{root_graph_pipeline}}).}

\item{unit}{One of \code{"cm"}, \code{"inch"}, \code{"px"}.}

\item{dpi}{Scan resolution (dots per inch).}

\item{length_method}{\code{"polyline"} (sqrt(2) chain code) or \code{"kimura"}.}
}
\value{
\code{et} with length/diameter columns in \code{unit}; records
  \code{unit}, \code{dpi}, \code{length_method} as attributes.
}
\description{
Pixels convert as \code{inch = px / dpi} and \code{cm = px * 2.54 / dpi}.
\code{length} is set to the chosen method; \code{length_poly} and
\code{length_kimura} are both retained in the same unit.
}
\examples{
\dontrun{
et <- root_graph_pipeline(skel, mask)
et_cm <- convert_root_units(et, unit = "cm", dpi = 300)
}
}
